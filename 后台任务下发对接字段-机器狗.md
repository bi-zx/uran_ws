# 后台任务下发对接字段-机器狗

本文档描述后台向机器狗 URAN 节点下发任务时需要使用的字段格式。按本文档顺序下发，ROS 端可以直接进入自动任务执行链路。

## 1. 执行前必须切到自动任务模式

后台在发送任务前，先发送 `control_switch`。

```json
{
  "msg_type": "control_switch",
  "msg_version": "1.0",
  "device_id": "cyberdog2_01",
  "timestamp_ms": 1779548815730,
  "switch": {
    "control_mode": "auto",
    "controller": "auto",
    "primary_uplink_protocol": "mqtt"
  }
}
```

字段说明：

| 字段 | 必填 | 说明 |
|---|---:|---|
| `msg_type` | 是 | 固定为 `control_switch` |
| `device_id` | 是 | 目标机器狗设备 ID |
| `timestamp_ms` | 是 | 后台发送时间，毫秒 |
| `switch.control_mode` | 是 | 执行自动任务时必须为 `auto` |
| `switch.controller` | 是 | 自动任务控制来源，必须为 `auto` |
| `switch.primary_uplink_protocol` | 否 | 当前建议为 `mqtt` |

注意：如果不先切到 `control_mode=auto`，`uran_autotask` 会拒绝执行任务；如果 `controller` 不是 `auto`，后续速度控制可能被 `uran_move` 拒绝。

## 2. 下发单台机器狗任务

后台随后发送 `task_ctrl`。顶层 `task_type` 和 `task_params_json.task_type` 都必须写成 `mission_planner_route`。

```json
{
  "msg_type": "task_ctrl",
  "msg_version": "1.0",
  "device_id": "cyberdog2_01",
  "task_id": "mp_interactive_plan_2e55b1e0a0_cyberdog2_01",
  "action": "start",
  "task_type": "mission_planner_route",
  "timestamp_ms": 1779548815730,
  "task_params_json": {
    "schema_version": "1.2.0",
    "task_type": "mission_planner_route",
    "task_id": "mp_interactive_plan_2e55b1e0a0_cyberdog2_01",
    "robot_id": "cyberdog2_01",
    "planner_run_id": "mp_interactive_plan_2e55b1e0a0",
    "projection_origin": {
      "lat": 31.230400,
      "lon": 121.473700,
      "alt": 0.0
    },
    "home_pose": {
      "id": "home",
      "name": "出发点",
      "lat": 31.230400,
      "lon": 121.473700,
      "alt": 0.0
    },
    "start_pose": {
      "id": "start",
      "name": "任务起点",
      "lat": 31.230410,
      "lon": 121.473710,
      "alt": 0.0
    },
    "route": {
      "points": [
        {
          "seq": 1,
          "id": "inspect_001",
          "name": "巡检点1",
          "point_type": "inspection",
          "lat": 31.230420,
          "lon": 121.473720,
          "alt": 0.0,
          "heading_deg": 0.0,
          "hover_time_s": 0.0,
          "actions": []
        },
        {
          "seq": 2,
          "id": "inspect_002",
          "name": "巡检点2",
          "point_type": "inspection",
          "lat": 31.230450,
          "lon": 121.473760,
          "alt": 0.0,
          "heading_deg": 0.0,
          "hover_time_s": 0.0,
          "actions": [
            {
              "type": "capture_image",
              "camera": "front",
              "label": "巡检拍照"
            }
          ]
        }
      ]
    }
  }
}
```

字段说明：

| 字段 | 必填 | 说明 |
|---|---:|---|
| `msg_type` | 是 | 固定为 `task_ctrl` |
| `device_id` | 是 | 目标机器狗设备 ID |
| `task_id` | 是 | 本次单台机器狗任务 ID，全局唯一 |
| `action` | 是 | 启动任务时固定为 `start` |
| `task_type` | 是 | 固定为 `mission_planner_route` |
| `timestamp_ms` | 是 | 后台发送时间，毫秒 |
| `task_params_json.schema_version` | 是 | 当前建议 `1.2.0` |
| `task_params_json.task_type` | 是 | 固定为 `mission_planner_route` |
| `task_params_json.projection_origin` | 是 | 经纬度投影原点，用于把 GPS 经纬度转成本地平面坐标 |
| `task_params_json.home_pose` | 是 | 机器狗 home 校准点，必须包含经纬度 |
| `task_params_json.start_pose` | 是 | 任务起点，必须包含经纬度 |
| `task_params_json.route.points` | 是 | 单台机器狗实际要执行的巡检点，不要放全量巡检点 |
| `route.points[].lat/lon` | 是 | 巡检点经纬度 |
| `route.points[].point_type` | 建议 | 巡检点写 `inspection` |
| `route.points[].actions` | 否 | 到点动作，没有动作时传空数组 |

注意：`task_params_json` 在 MQTT 包里可以是对象，也可以是 JSON 字符串。推荐后台直接传对象，`uran_core` 会转为 ROS 端需要的字符串。

## 3. 后台如何判断任务结束

任务完成后，ROS 端会上行 `task_progress`。

后台需要监听 MQTT 上行包：

```json
{
  "msg_type": "uplink_data",
  "msg_version": "1.0",
  "device_id": "cyberdog2_01",
  "source_pkg": "uran_autotask",
  "data_type": "task_progress",
  "timestamp_ns": 1779549000000000000,
  "payload": {
    "task_id": "mp_interactive_plan_2e55b1e0a0_cyberdog2_01",
    "stage": "completed",
    "status": "normal",
    "progress_percent": 100.0,
    "event": "task_completed",
    "error": null
  }
}
```

判断条件：

| 条件 | 含义 |
|---|---|
| `data_type == "task_progress"` | 这是任务进度包 |
| `payload.stage == "completed"` | 任务已完成 |
| `payload.status == "normal"` | 正常完成 |
| `payload.event == "task_completed"` | 完成事件 |

## 4. 任务结束后切回手动模式

后台收到任务完成消息后，应再发送一次 `control_switch`，把机器狗切回手动模式。

```json
{
  "msg_type": "control_switch",
  "msg_version": "1.0",
  "device_id": "cyberdog2_01",
  "timestamp_ms": 1779549001000,
  "switch": {
    "control_mode": "manual",
    "controller": "cloud",
    "primary_uplink_protocol": "mqtt"
  }
}
```

说明：

| 字段 | 值 | 说明 |
|---|---|---|
| `switch.control_mode` | `manual` | 退出自动任务模式 |
| `switch.controller` | `cloud` | 交回后台手动控制 |

如果现场手持端接管，则 `controller` 可以按业务改为 `field`。

## 5. 后台随时打断任务

后台随时可以发送 `task_ctrl` 的 `stop` 动作。ROS 端收到后会停止本地速度控制，发布零速停止命令，并上报任务中止状态。

```json
{
  "msg_type": "task_ctrl",
  "msg_version": "1.0",
  "device_id": "cyberdog2_01",
  "task_id": "mp_interactive_plan_2e55b1e0a0_cyberdog2_01",
  "action": "stop",
  "task_type": "mission_planner_route",
  "timestamp_ms": 1779548900000,
  "task_params_json": {
    "task_type": "mission_planner_route"
  }
}
```

后台收到的中止上行包通常类似：

```json
{
  "msg_type": "uplink_data",
  "data_type": "task_progress",
  "payload": {
    "task_id": "mp_interactive_plan_2e55b1e0a0_cyberdog2_01",
    "stage": "aborted",
    "status": "exception",
    "event": "stopped_by_command",
    "error": {
      "code": "E_TASK_STOPPED",
      "description": "task stopped by external command",
      "suggested_action": "restart_task"
    }
  }
}
```

如果后台希望“暂停后还能恢复”，可以用 `pause` 和 `resume`：

```json
{
  "msg_type": "task_ctrl",
  "msg_version": "1.0",
  "device_id": "cyberdog2_01",
  "task_id": "mp_interactive_plan_2e55b1e0a0_cyberdog2_01",
  "action": "pause",
  "task_type": "mission_planner_route",
  "timestamp_ms": 1779548900000,
  "task_params_json": {
    "task_type": "mission_planner_route"
  }
}
```

```json
{
  "msg_type": "task_ctrl",
  "msg_version": "1.0",
  "device_id": "cyberdog2_01",
  "task_id": "mp_interactive_plan_2e55b1e0a0_cyberdog2_01",
  "action": "resume",
  "task_type": "mission_planner_route",
  "timestamp_ms": 1779548910000,
  "task_params_json": {
    "task_type": "mission_planner_route"
  }
}
```

恢复前必须仍处于：

```text
control_mode = auto
controller = auto
```

## 6. ROS 端本地调试命令

本地切到自动任务模式：

```bash
ros2 topic pub --once /uran/core/switch/mode uran_msgs/msg/ModeSwitchCmd "{control_mode: auto, controller: auto, timestamp_ns: 0}"
```

本地启动任务：

```bash
ros2 topic pub --once /uran/core/downlink/task_ctrl uran_msgs/msg/TaskCtrlCmd "{msg_version: '1.0', task_id: 'local_route_001', action: 'start', task_type: 'mission_planner_route', task_params_json: '{\"schema_version\":\"1.2.0\",\"task_type\":\"mission_planner_route\",\"task_id\":\"local_route_001\",\"projection_origin\":{\"lat\":31.2304,\"lon\":121.4737,\"alt\":0.0},\"home_pose\":{\"id\":\"home\",\"lat\":31.2304,\"lon\":121.4737,\"alt\":0.0},\"start_pose\":{\"id\":\"start\",\"lat\":31.23041,\"lon\":121.47371,\"alt\":0.0},\"route\":{\"points\":[{\"seq\":1,\"id\":\"p1\",\"point_type\":\"inspection\",\"lat\":31.23042,\"lon\":121.47372,\"alt\":0.0,\"actions\":[]}]}}', timestamp_ns: 0}"
```

本地停止任务：

```bash
ros2 topic pub --once /uran/core/downlink/task_ctrl uran_msgs/msg/TaskCtrlCmd "{msg_version: '1.0', task_id: 'local_route_001', action: 'stop', task_type: 'mission_planner_route', task_params_json: '{\"task_type\":\"mission_planner_route\"}', timestamp_ns: 0}"
```

本地暂停任务：

```bash
ros2 topic pub --once /uran/core/downlink/task_ctrl uran_msgs/msg/TaskCtrlCmd "{msg_version: '1.0', task_id: 'local_route_001', action: 'pause', task_type: 'mission_planner_route', task_params_json: '{\"task_type\":\"mission_planner_route\"}', timestamp_ns: 0}"
```

本地恢复任务：

```bash
ros2 topic pub --once /uran/core/downlink/task_ctrl uran_msgs/msg/TaskCtrlCmd "{msg_version: '1.0', task_id: 'local_route_001', action: 'resume', task_type: 'mission_planner_route', task_params_json: '{\"task_type\":\"mission_planner_route\"}', timestamp_ns: 0}"
```

本地切回手动模式：

```bash
ros2 topic pub --once /uran/core/switch/mode uran_msgs/msg/ModeSwitchCmd "{control_mode: manual, controller: cloud, timestamp_ns: 0}"
```

查看任务状态：

```bash
ros2 service call /uran/autotask/status uran_srvs/srv/GetTaskStatus "{task_id: ''}"
```

查看后台下发到 ROS 的任务包：

```bash
ros2 topic echo /uran/core/downlink/task_ctrl
```

查看任务上行包：

```bash
ros2 topic echo /uran/core/uplink/data
```
